<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- مطلوب فقط لحفظ الصور المُنزَّلة في المعرض على أندرويد 9 فما دون
         (على أندرويد 10 فما فوق يتم الحفظ عبر MediaStore بدون أي إذن). -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- allowBackup معطّل: النسخ الاحتياطي التلقائي لأندرويد (adb backup / نسخ Google
         الاحتياطي) قد يشمل ملفات SharedPreferences وقواعد بيانات محلية للمصادقة
         (Firebase Auth session)، ما يفتح مسارًا لسرقة الجلسة إن استُعيد نسخة احتياطية
         على جهاز آخر. -->
    <application
        android:name=".OpouApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OPOU"
        android:networkSecurityConfig="@xml/network_security_config"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="34"
        xmlns:tools="http://schemas.android.com/tools">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.OPOU"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- اختصارات التطبيق الثابتة (الضغط المطوّل على أيقونة أوبو) — راجع res/xml/shortcuts.xml -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- شاشة عرض نص الانهيار الكامل (راجع util/CrashReporter.kt). عملية منفصلة
             حتى تعمل حتى إن كان الانهيار الأصلي سبّبه تلف حالة العملية الرئيسية. -->
        <activity
            android:name=".util.CrashDetailsActivity"
            android:exported="false"
            android:process=":crash_report"
            android:theme="@android:style/Theme.Material.NoActionBar" />

        <service
            android:name=".service.OpouMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
